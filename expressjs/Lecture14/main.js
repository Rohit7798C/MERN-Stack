// First of all we have to create a package.json file 
// command : npm init

// Install the npm package
// command : npm install

// Install the express-generator
// command: npm install express-generator

// Display the command options with -h option
// command: express -h

// Creating a Dummy app using the View Engine as : Pug
// command: express --view=pug myapp

// Here we are creating the app using the File Engine : EJS
// command: express --view=ejs myapp

// This command creates the Directory named ejsapp
// This Directory contains multiple folders

// If a folder named node_modules is absent then we have to install the http-errors module
// command: npm install http-errors

// If we want to run the file app.js for the app we use the command 
// command: set DEBUG=ejsapp:* & npm start

// We can make changes to the app and run with the above command
